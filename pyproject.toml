[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "antares"
version = "0.1.5a"
description = "Equation-Oriented Simulation Framework"

[tool.setuptools.packages.find]
where = ["src"]
